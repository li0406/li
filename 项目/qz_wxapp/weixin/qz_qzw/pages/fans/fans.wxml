<view class="fan-list">
  <view class="fan" wx:for="{{fanList}}" wx:for-item="item" wx:key="{{item.uuid}}">
    <image src="{{item.avatar?item.avatar:'../../img/default-head.png'}}"></image>
    <view class="name">{{item.nickname}}</view>
  </view>
</view>